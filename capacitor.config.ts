import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.hightracks',
  appName: 'HighTracks',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
